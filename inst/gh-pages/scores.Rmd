---
title: "Scores"
output:
  html_document:
    toc: true
    toc_float: true
---

```{r, echo=F, warning=FALSE, message=F}
source("./_site.R")
library(ohicore) # devtools::install_github('ohi-science/ohicore')
```

Scores displayed below are based on template data that were extracted from the global assessment. These scores will update when data layers are modified.

```{r}
scores = read_csv(file.path(dir_scenario, 'scores.csv')) # View(scores)

for (rgn_id in sort(unique(scores$region_id))[1:3]){ # rgn_id = sort(unique(scores$region_id))[1]
  #d = scores %>%
  #  filter(region_id == rgn_id, dimension=='score')
  lengths <- sample(1:10)
  widths <- sample(1:10)
  labels <- paste(LETTERS[1:10], "X", sep="")

  PlotFlower(lengths, widths, labels)
}
```

