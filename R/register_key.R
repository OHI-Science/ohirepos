## adds registered key to the list in ohi-webapps.
## change the names of these headers once in new branch of ohi-webapps.

register_key <- function(key,
                         study_area,
                         gl_rgn_id,
                         gl_rgn_name,
                         dir_shp_file,
                         default_scenario,
                         year) {

}
